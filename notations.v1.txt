
1. код очень тяжело читать, потому что не выдержаны правила оформления и именования.
по оформлению почитай zend coding standarts или вот это:
http://uat.sb.htc-cs.com/profile/edit/.
по именованию - имя переменной должно отражать то, что она в себе хранит. 
пусть оно даже будет длинным, в современных редакторах это не проблема.
> Переписал согласно zend coding standarts

2. метод getanswer получился ужасающе огромным)) есть такое негласное правило - если метод занимает больше одного экрана,
стоит подумать о том, чтобы разбить его на несколько более мелких.
> Разбил и вынес в отдельные функции повторяющиеся куски

3. по поводу проектирования. сейчас у тебя класс calculus реализован просто, "чтобы был" (по крайней мере так выглядит).
возможно, задача и не стоит того, чтобы строить супер-архитектуру, но я бы все-таки предложил тебе поправить))
в идеале, каждый класс должен нести в себе одну конкретную функцию. например, я бы выделил такие классы:
 - контроллер (работа с http-контекстом: обрабатывает запрос, формирует ответ)
 - синтаксический анализатор (парсит строку и предоставляет структуру для вычисления)
 - собственно калькулятор
может быть, что-то еще захочется вынести - например, структуру для вычисления.
> сделано. Выделил класс для базовых вычислений, класс для модификаторов, класс для работы с http-контекстом

4. классы лучше подгружать через __autoload.
> сделано, через spl_autoload_register. Причина http://php.net/manual/ru/language.oop5.autoload.php

5. в возвращаемом json лучше разделить поле, хранящее результат вычисления, и поле с сообщением об ошибке.
и можно добавить поле со статусом операции (выполнилась или нет).
>сделано.


[27.03.2012 16:57:18] dmitriy korablinov: по html/css/js-коду нужны коментарии?
[27.03.2012 17:06:10] dmitriy korablinov: по html

1. забудь про html 4)))) вот нормальный доктайп: <!doctype html>
>исправил.

2. стили подгружать надо через <link/>. джаваскрипты лучше подключать не в <head>, как пишут в учебниках:),  а в самом конце <body>
>исправил.


3. я лично очень не люблю вставку скриптов инлайн, типа вот этого: onclick="digitbutton('0'); return false;
jquery позволяет все обработчики навесить во внешних файлах скриптов

4. разнобой со схемой именования: классы с подчеркиванием, ид - camelcase, name - воттак:)

[27.03.2012 17:07:01] dmitriy korablinov: в css вложенность элементов лучше отражать отступами
>Исправил. Надеюсь, понял правильно